#include <stdio.h>
#include <stdlib.h>

int main(int argc, char* argv[]) {
	printf("ola pessoas do zenith :) vcs são lindos <3\n");

	int a = atoi(argv[1]);
	int b = atoi(argv[2]);
	printf("%d + %d = %d\n", a, b, a+b);
}
